{
  "name": "next-performance-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit && vite build",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.26",
    "@types/node": "^24.3.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
